services:
  beatsync:
    build: .
    image: beatsync-engine
    container_name: beatsync-engine
    volumes:
      - ./media:/app/media
      - ./output:/app/output
      - ./examples:/app/examples:ro
    command: ["--config", "examples/drill.json"]
